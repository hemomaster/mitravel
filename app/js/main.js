"use strict";

document.addEventListener("DOMContentLoaded", () => {
  const swiper = new Swiper(".slider__container", {
    centeredSlides: true,
    slidesPerView: "auto",
    loop: true,
    spaceBetween: 100,
  });
});
